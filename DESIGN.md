# DataStore API – מסמך תכנון, שלב ראשון

מסמך זה מסכם את ההחלטות שהתקבלו בשלב התכנון. הוא הבסיס המוסכם לפיתוח.
אין בו קוד. כל שינוי בהחלטה כאן צריך להתעדכן במסמך לפני שהוא מיושם.

## 1. מטרה

שירות אחסון ושליפה של JSON וקבצים עבור סוכני AI, בלי הרשמה ובלי חשבון.
כל פעולה בתשלום משולמת בקריאה אחת דרך HTTP 402 בפרוטוקול x402.

## 2. עקרונות

- **אין חשבונות, אין טוקנים, אין יתרה.** כל תשלום קונה דבר מוחשי לפריט אחד.
- **כל פריט הוא יחידה עצמאית.** מזהה, סוד גישה, תאריך תפוגה, מונה קריאות.
- **אין שחזור.** סוד או סיסמה שאבדו אינם ניתנים לשחזור, כי אי אפשר לזהות את הסוכן.
- **פרטי כברירת מחדל.** אין תוכן פומבי בשלב ראשון.
- **שליפה מלאה בלבד.** אין שאילתות, אין סינון, אין רשימות.
- **ספק אחד, בלי מסד נתונים.** תוכן ומטא-דאטה ב-R2 בלבד.

## 3. פעולות ה-API

| פעולה | תשלום | הערות |
|---|---|---|
| העלאת JSON או קובץ | כן, $0.01 | עד 25 MB, זמן חיים 1–365 ימים, סוגים מרשימת היתר |
| שליפה מלאה | לא, יורד מהמכסה | עם סוד, קישור חתום או סיסמה. JSON מוחזר inline, קובץ כ-attachment |
| סטטוס פריט | לא | תפוגה, קריאות שנותרו, גודל, סוג. לא נספר כקריאה |
| הארכת זמן חיים | כן, $0.01 | תאריך תפוגה חדש, עד שנה מהיום |
| רכישת חבילת קריאות | כן, $0.01 | 1,000 קריאות לפריט. לא דורש סוד, כל מי שמכיר את המזהה יכול לשלם |
| יצירת קישור חתום | לא | תפוגה מוגדרת, עד תפוגת הפריט |
| הגדרת סיסמה לשיתוף | לא | 12–128 תווים |
| מחיקה מוקדמת | לא | ללא החזר. מבטלת גם את כל הקישורים החתומים |

לא כלול בשלב ראשון: רשימת פריטים, שאילתות, עדכון חלקי, שיתוף מרחב בין סוכנים,
ממשק אנושי, העלאה בחלקים, קישורים פומביים, MCP server.

## 4. נקודות קצה

כל הנתיבים תחת `/v1`. שני דומיינים: `api.` לסוכנים, `dl.` לקישורי שיתוף.
אימות הבעלים: `Authorization: Bearer <secret>`. תשלום: x402 בכותרת התשלום.
פרמטרים של בקשה עוברים ב-query או בגוף JSON, לא בכותרות מותאמות. הכותרת
המותאמת היחידה בבקשה היא `Idempotency-Key`, שהיא מוסכמה מוכרת.

### תיעוד, חינם

| נתיב | תיאור |
|---|---|
| `GET /llms.txt` | הסבר השירות לסוכן בקריאה אחת: מה זה, מחירים, דוגמת קריאה אחת לכל פעולה |
| `GET /openapi.json` | מפרט OpenAPI |
| `GET /tools.json` | הגדרות כלים ל-function calling בפורמט JSON Schema: `store`, `retrieve`, `status`, `extend`, `share_link`, `delete`, `feedback`. מפתח מעתיק לתוך הסוכן שלו |
| `GET /v1/pricing` | `{"upload": "0.01", "extend": "0.01", "reads_1000": "0.01", "currency": "USDC", "network": "base", "max_bytes": 26214400, "max_ttl_days": 365, "included_reads": 100}` |
| `GET /terms` | תנאי שימוש ופרטיות |

### פריטים

| נתיב | אימות | תשלום | תיאור |
|---|---|---|---|
| `POST /v1/items?ttl_days=7&label=...` | אין | $0.01 | העלאה. הגוף הוא התוכן כמו שהוא. `ttl_days` 1–365, ברירת מחדל 7. `label` אופציונלי, עד 100 תווים, לזיהוי הפריט על ידי הסוכן. `Content-Type` אופציונלי: אם חסר או `application/octet-stream`, הסוג מזוהה מהתוכן. כותרת `Idempotency-Key` מומלצת |
| `GET /v1/items/{id}` | סוד | לא, קריאה אחת. כשהמכסה נגמרה: 402, ותשלום של $0.01 על אותה בקשה מוסיף 1,000 קריאות ומחזיר את התוכן | התוכן. JSON כ-`application/json`, קובץ כ-attachment בסוג שזוהה. כותרות `X-Reads-Remaining`, `X-Expires-At` |
| `GET /v1/items/{id}/status` | סוד | לא, לא נספר | אובייקט הפריט המלא (ראו להלן), בלי `secret` |
| `DELETE /v1/items/{id}` | סוד | לא | מחיקה מיידית. 204. ללא החזר |
| `POST /v1/items/{id}/extend` | סוד | $0.01 | גוף `{"ttl_days": N}`. תפוגה חדשה = היום + N, עד 365. אם התאריך החדש מוקדם מהנוכחי: 400 בלי חיוב. מחזיר את אובייקט הפריט |
| `POST /v1/items/{id}/reads` | אין | $0.01 | מוסיף 1,000 קריאות. מחזיר את אובייקט הפריט |

### אובייקט הפריט

כל תשובת JSON על פריט מחזירה את אותו מבנה, כדי שהסוכן ילמד צורה אחת:

```json
{
  "id": "itm_7f3a...",
  "secret": "sk_...",                 // רק בתשובת ההעלאה, פעם אחת
  "label": "crawl results 2026-09-10",
  "content_type": "application/json",
  "size": 48213,
  "created_at": "2026-09-10T12:00:00Z",
  "expires_at": "2026-09-17T12:00:00Z",
  "reads_remaining": 100,
  "has_password": false,
  "links": {
    "self":    "https://api.../v1/items/itm_7f3a",
    "status":  "https://api.../v1/items/itm_7f3a/status",
    "extend":  "https://api.../v1/items/itm_7f3a/extend",
    "reads":   "https://api.../v1/items/itm_7f3a/reads",
    "share":   "https://api.../v1/items/itm_7f3a/links",
    "delete":  "https://api.../v1/items/itm_7f3a"
  },
  "payment": {                        // רק אחרי סליקה
    "amount": "0.01", "currency": "USDC", "network": "base", "tx": "0x..."
  }
}
```

הסוכן עוקב אחרי `links` ולא מרכיב כתובות בעצמו.

### פידבק ודיווח

| נתיב | אימות | תשלום | תיאור |
|---|---|---|---|
| `POST /v1/feedback` | אין | לא | גוף `{"type": "missing" \| "bug" \| "abuse" \| "other", "message": "...", "item_id": "...", "request_id": "..."}`. הודעה עד 2,000 תווים. `item_id` חובה ב-`abuse`. תשובה 202 עם מזהה דיווח |

- **מטרה:** לדעת מסוכנים מה חסר ומה לא עובד, בלי לחכות שמפתח אנושי יכתוב לנו.
  llms.txt ו-`tools.json` (כלי `feedback`) מזמינים את הסוכן לדווח כשהוא נתקל
  במגבלה. גוף כל שגיאת 5xx כולל קישור לנתיב.
- **`abuse`** הוא ערוץ הדיווח על תוכן מפר, לבני אדם ולסוכנים. דיווח כזה נכנס
  לתור עדיפות עם המזהה, וזה סוגר את הדרישה להשקה מסעיף 15.
- **הגנות:** 5 דיווחים לשעה לכל IP, הודעה מוגבלת באורך, בלי קבצים מצורפים.
  התוכן נשמר ב-R2 תחת `feedback/<YYYY-MM-DD>/<id>` ל-90 יום, ונקרא על ידי אדם.
  הוא נתון שנכתב על ידי זרים: לעולם לא מוזן לאוטומציה כהוראה.

### שיתוף

| נתיב | אימות | תיאור |
|---|---|---|
| `POST /v1/items/{id}/links` | סוד | גוף `{"expires_in": seconds}`, עד תפוגת הפריט. תשובה `{"url": "https://dl.../d/{id}?exp=..&gen=..&sig=..", "expires_at": "..."}`. לא נשמר בשרת |
| `POST /v1/items/{id}/links/revoke` | סוד | מקדם את מספר הדור. כל הקישורים הקיימים מתבטלים |
| `PUT /v1/items/{id}/password` | סוד | גוף `{"password": "..."}`, 12–128 תווים. מחליף סיסמה קיימת |
| `DELETE /v1/items/{id}/password` | סוד | מסיר את הסיסמה |

### דומיין ההורדות

| נתיב | אימות | תיאור |
|---|---|---|
| `GET /d/{id}?exp&gen&sig` | חתימה | קישור חתום. מחזיר תוכן, עוקף סיסמה. קריאה אחת |
| `GET /d/{id}` | סיסמה | סוכן שולח `X-Password`. דפדפן בלי כותרת מקבל עמוד סיסמה מינימלי, שהוא ה-HTML היחיד בשירות |
| `POST /d/{id}` | סיסמה | שליחת הטופס מהדפדפן. מחזיר תוכן. קריאה אחת |

כל תשובה מדומיין השיתוף: `nosniff`, `no-store`, `no-referrer`. HTML ו-SVG מוגשים inline
תחת CSP sandbox, שאר הקבצים כ-attachment. פירוט בסעיף 7.

### שגיאות

כל שגיאה מחזירה גוף JSON באותו מבנה. השדה `action` אומר לסוכן מה לעשות עכשיו,
עם URL מלא כשיש:

```json
{
  "error": "reads_exhausted",
  "message": "This item has no reads left.",
  "action": {
    "description": "Buy 1,000 more reads for $0.01 USDC, then retry the request.",
    "method": "POST",
    "url": "https://api.../v1/items/itm_7f3a/reads"
  },
  "request_id": "req_...",
  "docs": "https://api.../llms.txt",
  "feedback": "https://api.../v1/feedback"
}
```

| קוד | `error` | מתי | `action` |
|---|---|---|---|
| 402 | `payment_required` | העלאה, הארכה, חבילה. הגוף לפי x402 בתוספת השדות למעלה | הסבר איך לשלם, קישור לתנאים, ב-testnet גם קישור ל-faucet |
| 402 | `reads_exhausted` | שליפה כשהמכסה נגמרה. הגוף לפי x402, כך שלקוח x402 משלם ומקבל את התוכן באותה קריאה | תשלום על אותה בקשה, או חבילה מראש בנתיב `reads`. הגוף כולל `expires_at` כדי שהסוכן ידע כמה זמן נשאר לפריט |
| 404 | `not_found` | פריט לא קיים, פג, סוד שגוי, חתימה לא תקינה או סיסמה שגויה. תשובה אחת לכולם | "הפריט לא זמין. אם אתה הבעלים, העלה מחדש" |
| 400 | `invalid_request` | פרמטר לא תקין, כולל הארכה לתאריך מוקדם מהנוכחי | תיקון הפרמטר, עם הערך הנוכחי בגוף |
| 409 | `idempotency_conflict` | אותו מפתח idempotency עם גוף שונה | שלח מפתח חדש |
| 413 | `too_large` | גוף מעל 25 MB | חלק או דחוס לפני העלאה |
| 415 | `unsupported_type` | סוג לא ברשימת ההיתר, או JSON לא תקין | רשימת הסוגים המותרים בגוף התשובה |
| 423 | `locked` | נעילת סיסמה, עם `Retry-After` | חכה |
| 429 | `rate_limited` | הגבלת קצב, עם `Retry-After` ו-`RateLimit-*` | חכה |

כל תשובה כוללת `X-Request-Id`. תשובה אחרי סליקה כוללת את כותרת התשובה של x402
עם מזהה העסקה, וגם את `payment` באובייקט הפריט.

### עקרונות ידידות לסוכן

- כל מה שהסוכן צריך להמשך נמצא בתשובה: מזהה, סוד, קישורים מלאים, תפוגה, מכסה.
- הסוכן לא צריך לדעת MIME, לא צריך להרכיב URL, ולא צריך לנחש מה לעשות אחרי שגיאה.
- מחירים וגבולות זמינים בנתיב חינם לפני שמשלמים.
- Testnet מריץ את אותו API בדיוק. סוכן מנסה בלי כסף ועובר ל-mainnet בשינוי דומיין.
- `tools.json` מאפשר לחבר את השירות לסוכן בלי לקרוא תיעוד.

## 5. תשלום

- **פרוטוקול:** x402. **Facilitator:** Coinbase CDP. **מטבע:** USDC על Base.
- **זרימה:** הבקשה הראשונה מקבלת 402 עם המחיר. ספריית הלקוח של x402 חוזרת
  על הבקשה עם כותרת התשלום. מבחינת הסוכן זו קריאה אחת.
- **מחיר קבוע לכל נתיב:** המחיר לא תלוי בגודל או בזמן החיים, ולכן תשובת ה-402
  זהה תמיד לאותה פעולה. זה בדיוק מודל ברירת המחדל של ספריות x402, בלי חישוב
  מותאם. הלקוח עשוי לשדר את גוף ההעלאה פעמיים, כי ספריות x402 שולחות את הבקשה
  המלאה בשני הסבבים. עד 25 MB זה מקובל.
- **סדר הפעולות בהעלאה:** אימות התשלום (חינם) → קריאת הגוף ובדיקות תקינות →
  כתיבה ל-R2 → סליקה → תשובה. כישלון בבדיקות תקינות (סוג אסור, JSON לא תקין,
  גודל מעל 25 MB) מחזיר שגיאה **בלי לסלוק**, כלומר הסוכן לא מחויב.
  אם הסליקה נכשלת אחרי הכתיבה, האובייקטים נמחקים והתשובה היא 402 מחדש.
  אם הכתיבה ל-R2 נכשלת, אין סליקה.
- **סדר הפעולות בהארכה ובחבילת קריאות:** אימות → בדיקת תקינות → **סליקה** →
  עדכון המטא-דאטה בכתיבה מותנית עם ניסיונות חוזרים. כאן סולקים קודם, כי העדכון
  הוא שינוי קטן שאפשר לחזור עליו עד שמצליח, בעוד שביטול סליקה בלתי אפשרי.
- **חלון התשלום:** דרישות ה-402 של נתיב ההעלאה מגדירות חלון של 10 דקות
  (`maxTimeoutSeconds`), כי העלאה של 25 MB בקו איטי, הבדיקות והכתיבה חייבים
  להסתיים לפני שהחתימה פגה. נתיבים אחרים: דקה.
- **קריאת הגוף:** בזרימה עם תקרה של 25 MB, לא לפי `Content-Length`, כי הוא עשוי
  להיות חסר או שקרי. חריגה עוצרת את הקריאה ומחזירה 413. תקינות JSON נבדקת
  במעבר זרימתי ולא ב-`JSON.parse`, כי 25 MB מפוענחים חורגים מזיכרון ה-Worker.
- **קבלה:** מזהה העסקה על הרשת מוחזר בכותרת התשובה. אין מערכת חשבוניות.
  מזהי סליקה נרשמים בלוג כדי לאפשר החזר ידני במקרה של כשל מצידנו.
- **תנאי שימוש:** קישור לתנאים חוזר בכל תשובת 402. תשלום מהווה הסכמה.
- **Idempotency, אוטומטית:** כל תשלום x402 נושא nonce ייחודי בחתימה. השרת שומר
  רשומה `idem/<wallet>/<nonce>` **לפני** הסליקה, עם מזהה הפריט, hash של הגוף
  והסוד מוצפן במפתח של השרת. אם התשובה אבדה והסוכן שולח שוב את אותה חתימה,
  השרת מזהה את הרשומה, בודק במטא-דאטה של הפריט אם הסליקה הושלמה (יש `tx`),
  ומחזיר את התוצאה המקורית בלי לסלוק שוב. אם הסליקה לא הושלמה, הוא ממשיך מהנקודה
  שבה נעצר. כך סוכן לעולם לא משלם ומאבד את הסוד, גם בלי לשלוח כותרת.
- **Idempotency, מפורשת:** כותרת `Idempotency-Key` אופציונלית מכסה את המקרה שבו
  קוד הסוכן חוזר על הבקשה עם תשלום חדש. הרשומה `idem/<wallet>/<key>` באותו מבנה.
  אותו מפתח עם hash גוף שונה מחזיר 409. הארנק מאומת מחתימת התשלום (אימות בלבד).
  רשומות idempotency נמחקות אחרי 24 עד 48 שעות דרך מנגנון התפוגה.
- **Testnet:** פריסה נפרדת לגמרי על Base Sepolia, עם bucket משלה, גבולות
  קטנים (1 MB, עד 7 ימים) ומחיקה יומית. הפריסה הראשית לא מקבלת תשלומי testnet,
  אחרת כל האחסון בחינם.

## 6. תמחור

שלוש פעולות בתשלום, מחיר אחד לכולן. אין נוסחה, אין תלות בגודל או בזמן חיים.

| פעולה | מחיר |
|---|---|
| העלאה (עד 25 MB, עד 365 ימים, כולל 100 קריאות) | $0.01 |
| הארכת זמן חיים (עד 365 ימים מהיום) | $0.01 |
| 1,000 קריאות נוספות | $0.01 |
| מחיקה מוקדמת | ללא החזר |

**למה זה עומד בעלויות:** המקרה הגרוע ביותר הוא 25 MB לשנה שלמה. האחסון ב-R2
עולה לנו $0.0044, הסליקה $0.001, סך הכל כ-$0.0055 מול הכנסה של $0.01. כל העלאה
קטנה או קצרה יותר רווחית בהרבה. עמלת הסליקה היא ההוצאה הדומיננטית ברוב
העסקאות, ולכן המחיר הוא פי 10 ממנה.

**למה מחיר אחיד ולא לפי גודל:** לסוכן קל להבין ולתעד ב-llms.txt במשפט אחד.
לשרת אין צורך לחשב מחיר מכותרות, ואין מקרה של אי-התאמה בין מוצהר לבפועל.
ל-x402 זה מודל ברירת המחדל. אם בעתיד יתברר שהרוב מעלים 25 MB לשנה,
אפשר להעלות את המחיר או להוסיף מדרגה אחת, בלי לשנות את ה-API.

### בסיס העלות (ספטמבר 2026)

| רכיב | עלות לנו |
|---|---|
| אחסון R2 | $0.015 לג'יגה-חודש |
| כתיבה ל-R2 (Class A) | $4.50 למיליון |
| קריאה מ-R2 (Class B) | $0.36 למיליון |
| הורדה מ-R2 | חינם |
| סליקת x402 בקוינבייס | $0.001 לסליקה אחרי 1,000 בחודש. אימות חינם |
| Workers | כ-$5 בחודש קבוע |

עמלת הסליקה היא ההוצאה הדומיננטית ברוב העסקאות. לכן כל פעולה בתשלום מתומחרת
פי 10 ממנה, וזה מכסה גם את המקרה הגרוע של אחסון.

## 7. גבולות

| פרמטר | ערך |
|---|---|
| גודל מקסימלי לפריט, JSON או קובץ | 25 MB |
| זמן חיים | 1 עד 365 ימים, ברירת מחדל 7 |
| תווית (`label`) | עד 100 תווים |
| סיסמה | 12 עד 128 תווים |
| ניסיונות סיסמה לפריט | 5 כשלונות ב-15 דקות ואז נעילה של 15 דקות. מצב הנעילה נשמר במטא-דאטה של הפריט |
| ניסיונות סיסמה ל-IP | 30 בדקה על כל הפריטים, דרך Rate Limiting binding של Workers |
| קצב API כללי ל-IP | ייקבע בפיתוח, תשובת 429 עם Retry-After |

### סוגי קבצים מותרים

JSON, HTML, SVG, טקסט, Markdown, CSV, PDF, PNG, JPEG, WebP, GIF.

- הזיהוי לפי תוכן (magic bytes), לא לפי סיומת או כותרת שהלקוח שלח. לטקסט,
  HTML ו-JSON אין magic bytes, ולכן הם נבדקים לפי תקינות: UTF-8 תקין, ול-JSON
  גם מבנה תקין.
- SVG נבדק כ-XML תקין עם אלמנט שורש `svg`, ומוגש בדיוק כמו HTML: inline תחת
  CSP sandbox. גם SVG יכול להכיל סקריפט, וה-sandbox מנטרל אותו באותו אופן.
- ZIP, קבצי הרצה וסקריפטים אסורים.
- JSON, HTML ו-SVG מוחזרים inline מדומיין ה-API כשהגישה עם הסוד, כי זה מה שסוכן
  צריך ודפדפן לא יכול לשלוח Bearer.

### הגשת HTML מדומיין השיתוף

HTML ו-SVG הם תוצר מרכזי של סוכנים (דוחות, דפים, גרפים), ולכן הם מוגשים
**inline** בקישורי שיתוף ולא כהורדה. הבידוד נעשה בכותרות, לא באיסור:

- `Content-Security-Policy: sandbox allow-scripts allow-forms allow-popups`.
  בלי `allow-same-origin`, ולכן כל דף רץ במקור אטום משלו: סקריפטים עובדים, אבל
  אין גישה ל-cookies, לאחסון, לדף הסיסמה או לדפים אחרים. זה מנטרל XSS בכותרת אחת.
- `Referrer-Policy: no-referrer`, כדי שקישור מתוך הדף לא ידליף את חתימת הקישור.
- `X-Content-Type-Options: nosniff` ו-`Cache-Control: no-store`.
- דומיין השיתוף לא מגדיר cookies בכלל.
- **דומיין השיתוף אינו תת-דומיין של המותג** אלא דומיין נפרד, כמו githubusercontent.
  אם תוכן מפר יגרום לסימון הדומיין בדפדפנים, זה לא פוגע ב-API ולא במותג.
- שאר הקבצים מדומיין השיתוף מוגשים כ-`Content-Disposition: attachment`.

הסיכון שנשאר הוא תוכן ולא טכני: דפי פישינג. לכן ערוץ הדיווח ותהליך ההסרה
הם תנאי להשקה ולא נושא פתוח.

## 8. מודל הנתונים ב-R2

לכל פריט שני אובייקטים:

- **תוכן:** `items/<id>` – ה-JSON או הקובץ כמו שהוא.
- **מטא-דאטה:** `meta/<id>` – hash של הסוד (SHA-256, הסוד אקראי וארוך), סוג, גודל,
  תווית, זמן יצירה, `expires_at` מדויק לשנייה, קריאות שנותרו, hash סיסמה אם הוגדרה,
  מצב נעילת סיסמה (מונה כשלונות וזמן), ומספר דור לקישורים חתומים.

**סיסמאות:** PBKDF2-SHA256 דרך WebCrypto, 600,000 איטרציות. לא Argon2, כי הוא
מבוסס זיכרון ואיטי ב-WASM בתוך Workers. הסיסמה ממילא באורך 12 תווים לפחות.

**תפוגה:**

- `expires_at` הוא זמן ההעלאה ועוד N ימים, מדויק. כל שליפה בודקת אותו, ולכן פריט
  שפג נחסם מיד גם אם עוד לא נמחק פיזית.
- מפתח סימון `expiry/<YYYY-MM-DD>/<id>` (אובייקט ריק) לפי תאריך התפוגה.
- Cron יומי עובר על הקידומת של אתמול ומוחק. **לפני כל מחיקה הוא קורא את
  המטא-דאטה ומוודא ש-`expires_at` באמת עבר.** הסימון הוא רמז, המטא-דאטה היא האמת.
  כך הארכה שהשאירה סימון ישן לא גורמת למחיקה מוקדמת. סימון שהתפוגה שלו עוד לא
  עברה נמחק כסימון בלבד, והפריט נשאר.
- בהארכה: כותבים `expires_at` חדש במטא-דאטה, יוצרים סימון חדש, ומוחקים את הישן.
  אם מחיקת הישן נכשלה, הבדיקה ב-Cron מכסה.
- אותו מנגנון משמש למחיקת רשומות idempotency אחרי 24 שעות.

## 9. מונה קריאות

**מונה רך.** בשליפה: קוראים מטא-דאטה, בודקים סוד ותפוגה ומכסה, מגישים את התוכן,
ומורידים את המונה ברקע אחרי שליחת התשובה בכתיבה מותנית (ETag) עם ניסיון חוזר.
בעומס מקבילי ייתכנו קריאות בודדות מעבר למכסה. זה מקובל.

- **נספר:** שליפה מוצלחת של תוכן, בכל אחת מדרכי הגישה.
- **לא נספר:** סטטוס, ניסיון סיסמה שנכשל, תשובות 402 או 404.
- כל תשובת שליפה כוללת כותרת עם מספר הקריאות שנותרו.
- כשהמכסה נגמרת, כל דרכי הגישה מקבלות 402 בפורמט x402. לקוח x402 משלם
  אוטומטית על אותה בקשה: השרת מוסיף 1,000 קריאות ומגיש את התוכן. זו קריאה אחת
  מבחינת הסוכן. אדם בדפדפן לא יכול לשלם, ולכן llms.txt ממליץ לסוכן לרכוש חבילה
  מראש בנתיב `reads` לפני שהוא משתף פריט עם אדם. רכישת חבילה לא דורשת סוד.
- הסוכן שולט בחיוב האוטומטי דרך תקרת התשלום של ספריית x402 שלו, כמו בכל שירות x402.
- שיפור עתידי אם יידרש: Durable Object לכל פריט. לא משנה את ה-API.

## 10. גישה ושיתוף

- **סוד הפריט:** מוחזר פעם אחת בהעלאה. מאפשר שליפה, סטטוס, הארכה, מחיקה,
  יצירת קישורים והגדרת סיסמה.
- **קישור חתום:** HMAC על מזהה, זמן תפוגה ומספר דור. לא נשמר בשרת. מאפשר
  שליפה בלבד ועוקף סיסמה, כי הבעלים יצר אותו. אי אפשר לבטל קישור בודד.
  פעולת "בטל את כל הקישורים" עם הסוד מקדמת את מספר הדור ומבטלת את כולם.
- **קישור עם סיסמה:** הסיסמה מוגדרת עם הסוד. ההורדה דורשת סיסמה.
- **סיסמה שנשכחה:** הסיסמה מגנה על הקוראים, לא על הבעלים. מחזיק הסוד תמיד
  יכול לדרוס אותה בלי לדעת את הישנה, להסיר אותה, ליצור קישור חתום שעוקף
  אותה, או לקרוא ישירות. לכן אין תהליך שחזור סיסמה: הקורא פונה למי ששיתף.
  רק אובדן הסוד הוא סופי.
- **הצפנה בצד הלקוח:** מתועדת כאפשרות. השירות לא רואה טקסט גלוי.
- **תשובה זהה** לסיסמה שגויה, לקישור חתום לא תקין ולפריט שלא קיים.
- **לוגים לא כוללים** query string או כותרות אימות, כי הם מכילים חתימות וסיסמאות.

## 11. בעלות, שמירה ומחיקה

- הבעלים של התוכן הוא מחזיק הסוד. השירות מקבל רישיון מוגבל לאחסן ולהגיש.
- השירות לא אחראי לתוכן ורשאי למחוק תוכן שמפר את תנאי השימוש.
- המחיקה בתפוגה סופית, ללא תקופת חסד. הסוכן יכול לשאול על התפוגה ולהאריך.
- אין גיבוי נפרד מעבר לעמידות של R2, כדי שמחיקה תהיה באמת מחיקה.
- לוגים: ראו סעיף 12.

## 12. לוגים וניטור

שלושה סוגי לוג, עם מטרות ותקופות שמירה שונות.

### א. לוג תפעולי – כל בקשה

| שדה | הערה |
|---|---|
| זמן, מזהה בקשה | מזהה הבקשה מוחזר לסוכן בכותרת תשובה, לצורך תמיכה |
| נתיב ופעולה | העלאה, שליפה, סטטוס, הארכה, חבילה, מחיקה, קישור, סיסמה |
| מזהה פריט | אם רלוונטי |
| תוצאה | קוד HTTP וסיבת דחייה (סוג אסור, מכסה נגמרה, תפוגה, סיסמה שגויה) |
| דרך גישה | סוד, קישור חתום, סיסמה |
| גודל, משך | בייטים ומילישניות |
| מדינה, IP מגובב | ה-IP מגובב עם מלח שמתחלף יומית. מספיק לזיהוי דפוסי ניצול, לא לזיהוי אדם |

- **מקום:** Workers Logs. שמירה 7 ימים, כלול בתוכנית Workers Paid עד 20 מיליון
  אירועים בחודש.
- **מטרה:** איתור תקלות, זיהוי ניצול לרעה, מענה לפניות תמיכה עם מזהה בקשה.

### ב. לוג תשלומים – רשומה חשבונאית

| שדה | הערה |
|---|---|
| זמן, מזהה בקשה | קישור ללוג התפעולי |
| סוג פעולה | העלאה, הארכה, חבילת קריאות |
| מזהה פריט | |
| כתובת ארנק משלם | מידע פומבי על הרשת ממילא |
| סכום ב-USDC, רשת | |
| מזהה עסקה על הרשת | ההוכחה |
| תוצאה | נסלק, נכשל, נסלק ואז נמחק בגלל כשל כתיבה |

- **מקום:** Logpush ל-bucket נפרד ב-R2 (`audit/`), עם 10 מיליון רשומות בחודש
  חינם. בנוסף, מזהה העסקה האחרון נשמר גם במטא-דאטה של הפריט.
- **שמירה:** 7 שנים, כמקובל לרשומות חשבונאיות. הרשומה שורדת את מחיקת הפריט.
- **מטרה:** התאמה מול הרשת, החזרים ידניים, דיווח הכנסות, ומיפוי ארנק ← פריטים
  לצורך טיפול בתוכן מפר או פנייה משפטית. זו הדרך היחידה במערכת לקשר בין פריטים
  לאותו משלם, כי אין חשבונות.

### ג. לוג אירועי מערכת

- מחיקות בתפוגה: כמה פריטים נמחקו בכל ריצת Cron, כמה דולגו כי התפוגה עוד לא
  עברה, כשלים.
- סליקה שנכשלה אחרי כתיבה, וניקוי האובייקטים.
- הסרת תוכן ביוזמתנו: מזהה פריט, סיבה, מי אישר. נשמר בלוג התשלומים, 7 שנים.
- נעילות סיסמה והפעלת הגבלת קצב: מזהה פריט ו-IP מגובב.
- דיווחי פידבק: נשמרים כתוכן ב-R2 ל-90 יום, ראו סעיף 4. דיווחי `abuse` נשמרים
  יחד עם ההחלטה עליהם 7 שנים.
- **מקום:** Workers Logs, 7 ימים, למעט הסרות תוכן.

### מה לעולם לא נרשם

תוכן של פריטים, סוד הפריט, סיסמאות (גם שגויות), חתימות של קישורים חתומים,
כותרת התשלום של x402 (מכילה חתימה), query strings, וכתובות IP גולמיות.

הערה: לוג ההפעלה האוטומטי של Workers רושם את ה-URL המלא כולל query. לכן
הוא מכובה, והלוג התפעולי נכתב במפורש מהקוד עם השדות שלמעלה בלבד.

### מדדים והתראות

- **מדדים** ב-Workers Analytics Engine: העלאות, הכנסה, תשובות 402, כשלי סליקה,
  שגיאות 5xx, משך ריצת Cron, סך אחסון. עלות זניחה בהיקף שלב ראשון.
- **התראות** על: שיעור כשלי סליקה, שיעור 5xx, Cron שלא רץ 36 שעות, וגידול
  חריג באחסון ביום אחד (סימן לניצול לרעה).

### פרטיות

מדיניות הפרטיות מציינת: IP מגובב לשבוע, כתובת ארנק ורשומת תשלום ל-7 שנים,
ושאין רישום של תוכן. כתובת ארנק היא פסאודונים פומבי ולא מזהה אדם בפני עצמה.

## 13. מחסנית

- Cloudflare Workers עם Hono.
- Cloudflare R2 לתוכן ולמטא-דאטה.
- Workers Cron למחיקה בתפוגה.
- ספריית x402 של Coinbase לצד השרת.
- Workers Logs ללוג תפעולי, Logpush ל-R2 ללוג תשלומים, Analytics Engine למדדים.
- שני דומיינים נפרדים לגמרי: אחד ל-API ואחד לשיתוף, שאינו תת-דומיין של המותג.

## 14. תיעוד וגילוי

- OpenAPI מלא.
- llms.txt שמסביר את השירות לסוכן בקריאה אחת.
- תנאי שימוש ומדיניות פרטיות, מקושרים מכל תשובת 402.
- רישום במדריכי x402 ו-MCP.

## 15. נשאר פתוח, לא חוסם פיתוח

- **תהליך הסרה של תוכן מפר: מי בודק, תוך כמה זמן, ומה נמחק. תנאי להשקה**
  בגלל הגשת HTML. ערוץ הדיווח עצמו הוא נתיב הפידבק בסעיף 4.
- שם ודומיין לשירות.
- הישות המשפטית שמאחורי תנאי השימוש וסמכות השיפוט.
- קצב API כללי לכל IP.
- מדיניות החזר ידני כשסליקה הצליחה והאחסון נכשל.

## 16. שלב שני, לא בהיקף הנוכחי

- קרדיט לפי כתובת ארנק (מתשלום x402), עם רשימת פריטים לפי ארנק.
- מדרגת מחיר שנייה לזמן חיים מעל 30 יום, רק אם לוג התשלומים יראה שזה נתח
  משמעותי בקבצים גדולים. נשקל ונדחה בשלב ראשון לטובת מחיר אחיד.
- MCP server.
- העלאה ישירה ל-R2 בקישור חתום לקבצים גדולים.
- שיתוף בין סוכנים עם הרשאות.
- ממשק אנושי לצפייה.
- קישורים פומביים.
- דוגמאות לספריות סוכנים נפוצות.
